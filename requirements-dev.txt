# Test kit — install with: pip install -r requirements-dev.txt
# The app itself needs no third-party packages; these are for the
# test/quality suite only.
pytest>=9.0
hypothesis>=6.150
pytest-cov>=7.0
pytest-mock>=3.15
pytest-timeout>=2.4
mutmut>=3.5
