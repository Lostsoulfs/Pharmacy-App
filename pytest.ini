[pytest]
testpaths = tests
# test_logic.py is the standalone 29-case regression runner (it uses
# its own check()/sys.exit harness). Run it directly with
# `python tests/test_logic.py`; pytest collects the rest.
addopts = --ignore=tests/test_logic.py
