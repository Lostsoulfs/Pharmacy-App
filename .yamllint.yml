---
extends: default

rules:
  document-start: disable
  new-lines: disable
  comments:
    min-spaces-from-content: 1
  line-length:
    max: 160
    level: warning
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
